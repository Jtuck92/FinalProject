import { UserPrvEventsPipe } from './user-prv-events.pipe';

describe('UserPrvEventsPipe', () => {
  it('create an instance', () => {
    const pipe = new UserPrvEventsPipe();
    expect(pipe).toBeTruthy();
  });
});
